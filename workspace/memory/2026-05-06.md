- Elliott wants repo changes split into discrete PRs and opened one at a time against `raid-guild/pinata-voice-cooking-companion`.
- Preferred GitHub identity for future repo work: `ecwireless-bot` using the provided PAT; avoid local bot identities like `pinniebot` for authorship.
- For architecture changes, do not add one-off workspace note files to PRs when a durable architecture/doc surface is more appropriate; update the relevant long-lived docs instead.
- During PR prep, explicitly guard against accidental inclusion of runtime/sync artifacts such as `.lifecycle/*`, `workspace/.openclaw/workspace-state.json`, generated audio files, DB WAL/SHM files, and unrelated `manifest.json` drift.
- PR sequence in progress:
  - PR #7: model-backed query interpreter (merged)
  - PR #8: missing recipe handling and recommendations (merged)
  - PR #9: query navigation controls (open at time of note)
